/* DO NOT MODIFY. Localizable created with localio. */

<% @segments.each do |term| %>
<%
  if term.is_comment?
%>
// <%= term.translation %>
<% else %>"<%= term.key %>" = "<%= term.translation %>";<%
    end
  end
%>
